---
name: vsf-hal-driver
description: |
  USE FOR: porting a new MCU to VSF HAL, generating driver skeleton, implementing VSF peripheral drivers (UART, I2C, SPI, GPIO), setting up IPCore-backed or direct-register peripherals.
  DO NOT USE FOR: build/flash/test loop (use vsf-board-run), board-level BSP changes, fixing driver bugs on already-supported chips.
---

# VSF HAL Driver Porting

## Overview

| Level | Goal | Verify |
|-------|------|--------|
| L0 | Vendor SDK UART echo works | serial output |
| L1 | VSF skeleton builds | cmake passes |
| L2 | First VSF UART works | vsf-board-run PASS |
| L3 | Other peripherals | per-peripheral test |

## L0 — SDK Baseline

Run minimal UART echo with vendor SDK. Do NOT proceed without serial output.

## L1 — VSF Skeleton

Collect from vendor SDK: CPU type, IRQ prio bits, clock freqs, per-peripheral IRQ numbers, register bases, IRQ handler names. Identify IPCore path (e.g. `ARM/PL011`).

Run `scripts/scaffold_chip.py --driver-dir <dir> --config chip_config.yml`. Script copies templates — no real driver logic. **Warning**: overwrites existing files.

Make it build: verify `device.h` macros, implement `driver.c` clock init, add SDK paths to `CMakeLists.txt`. Build with `vsf-board-run` (build step only, see that skill).

## L2 — First Peripheral (UART)

Reference: an existing UART driver for a supported chip (e.g. `driver/<VENDOR>/<DEVICE>/uart/uart.c`).

### Header (`chip/uart/uart.h`)

**Direct register mode**: set `REIMPLEMENT_TYPE_*=ENABLED`, define mode/irq/status/cfg enums, include `vsf_template_usart.h`.

**IPCore mode**: include IPCore header (types built-in).

### Source (`chip/uart/uart.c`)

**Direct**: define struct `{.reg, .isr}`, implement all API functions.

**IPCore**: `implement(vsf_pl011_usart_t)`, IPCore handles baudrate/reg/IRQ. Chip provides reset/NVIC/clock. Set `__VSF_HAL_${IP}_USART_CLASS_INHERIT__`.

Both instantiate with IMP_LV0:
```c
#define VSF_USART_CFG_IMP_LV0(ID, OP)                     \
    vsf_hw_usart_t vsf_hw_usart##ID = {.reg = REG, OP};   \
    void VSF_HW_USART##ID_IRQHandler(void) {               \
        vsf_pl011_usart_irqhandler(&vsf_hw_usart##ID.use); \
    }
#include "hal/driver/common/usart/usart_template.inc"
```

### Board (`vsf_board.c`)

Expose `vsf_usart_t *usart[N]`. Init: pinmux → reset → init → enable → irq.

### Application (`main.c`)

Pure VSF APIs only — no chip-specific code.

### Validation

Run `scripts/check-usart-*.py chip/uart/uart.{h,c}` (exit: 0=pass, 1=errors, 2=warnings). Then verify with `vsf-board-run`.

## L3 — Other Peripherals

Repeat L2 pattern per peripheral. Non-IPCore (GPIO): register ops directly. Verify each with `vsf-board-run`.

## USART Reference

### API (generated by `VSF_USART_APIS()` in `vsf_template_usart.h`)

Core: `init`, `fini`, `enable`, `disable`, `capability`, `get_configuration`.
IRQ: `irq_enable`, `irq_disable`, `irq_clear`, `status`.
FIFO: `rxfifo_get_data_count`, `rxfifo_read`, `txfifo_get_free_count`, `txfifo_write`.
DMA: `request_rx`, `request_tx`, `cancel_rx`, `cancel_tx`, `get_rx_count`, `get_tx_count`.
Control: `ctrl`.

### Mode bits (`vsf_usart_mode_t`)

Parity(0-2): NO/EVEN/ODD mandatory. Stop(3-4): 1_STOPBIT mandatory. Data(5-7): 8_BIT_LENGTH mandatory. HW ctrl(8). TX enable(9). RX enable(10). Sync clock(11). Half duplex(14). TX FIFO threshold(15-16). RX FIFO threshold(17-18).

### IRQ mask bits (`vsf_usart_irq_mask_t`)

TX_CPL(0), RX_CPL(1), TX(2), RX(3) — template defaults. RX_TIMEOUT(4), CTS(5), FRAME_ERR(6), BREAK_ERR(7), PARITY_ERR(8), RX_OVERFLOW_ERR(9), RX_IDLE(12) — need `#define VSF_USART_IRQ_MASK_<X>` in header or VSF treats as unsupported.

### Mandatory mode placeholders

Must exist even when HW lacks the feature:
```c
VSF_USART_9_BIT_LENGTH  = (0x1ul << 24),
VSF_USART_1_5_STOPBIT   = (0x1ul << 25),
VSF_USART_0_5_STOPBIT   = (0x1ul << 26),
VSF_USART_10_BIT_LENGTH = (0x1ul << 27),
VSF_USART_SYNC_CLOCK_ENABLE = (0x1ul << 28),
VSF_USART_SYNC_CLOCK_DISABLE = 0,
VSF_USART_HALF_DUPLEX_ENABLE = (0x1ul << 29),
VSF_USART_HALF_DUPLEX_DISABLE = 0,
```

### IRQ handler pattern

```c
void VSF_HW_USART<N>_IRQHandler(void) {
    uintptr_t ctx = vsf_hal_irq_enter();
    // 1. Read raw status 2. Read enabled mask 3. active = raw & mask
    // 4. Clear active irqs 5. If active && handler_fn: call it
    vsf_hal_irq_leave(ctx);
}
```

### Template files

| File | Purpose |
|------|---------|
| `source/hal/driver/common/template/vsf_template_usart.h` | API declarations, default types |
| `source/hal/driver/template/__series_name_a__/common/usart/usart.h` | Header skeleton |
| `source/hal/driver/template/__series_name_a__/common/usart/usart.c` | Implementation skeleton |
| `source/hal/driver/common/usart/usart_template.inc` | IMP_LV0 instantiation |
| `source/hal/driver/common/usart/usart_interface.c` | Multi-class dispatch |
| `source/hal/driver/common/usart/usart_common.c` | Default implementations |

### Reference implementations

- **RP2040 (PL011 IPCore)**: `source/hal/driver/RaspberryPi/RP2040/uart/uart.c`
- **PL011 IPCore**: `source/hal/driver/IPCore/ARM/PL011/vsf_pl011_uart.{h,c}`
- **Template (direct)**: `source/hal/driver/template/__series_name_a__/common/usart/usart.c`
- More: `source/hal/driver/ST/STM32H7RSXX/common/usart/usart.c`, `source/hal/driver/GigaDevice/GD32H7XX/common/usart/usart.c`

## Troubleshooting

| Symptom | Fix |
|---------|-----|
| Build fails | Verify macros match SDK, clock init, SDK paths in CMakeLists.txt |
| No UART output | Deassert reset in `_init()`, check `CFG_IMP_LV0` IRQ wiring |
| Garbage output | Wrong baudrate formula |
| Spurious IRQ | Check mask before FIFO read |
| RX not firing | Use `RX_FIFO_THRESHOLD_NOT_EMPTY` |
| Stream hangs | Drain RX FIFO when full |
